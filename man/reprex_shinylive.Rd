% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/reprex_shinylive.R
\name{reprex_shinylive}
\alias{reprex_shinylive}
\title{Create a shinylive.io Share URL from Shiny app code}
\usage{
reprex_shinylive(
  x = NULL,
  input = NULL,
  base_url = "https://shinylive.io/r/app/",
  html_preview = TRUE,
  copy_to_clipboard = TRUE
)
}
\arguments{
\item{x}{An expression. If not provided, will try `input`, then clipboard.}

\item{input}{Character. If length 1 and a file exists at that path, code is read from file. Otherwise, assumed to be code as character vector.}

\item{base_url}{The base shinylive.io URL for sharing (default: "https://shinylive.io/r/app/").}

\item{html_preview}{Logical. Whether to show rendered output in a viewer (RStudio or browser). Always FALSE in a noninteractive session.}

\item{copy_to_clipboard}{Logical. Whether to copy the URL to the clipboard (default: TRUE, only if interactive and clipr available).}
}
\value{
A single string containing the shareable URL.
}
\description{
Prepares Shiny app code for sharing and execution in a shinylive.io browser session by encoding it into a URL. Accepts code as an expression, character vector, file path, or from the clipboard, similar to reprex_shinylive().
}
\examples{
\dontrun{
reprex_shinylive({
  library(shiny)
  ui <- fluidPage("Hello, world!")
  server <- function(input, output, session) {}
  shinyApp(ui, server)
})
}
}
